import { StyleSheet } from 'react-native'

const mainStyles = StyleSheet.create({
  mainBackground: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'stretch',
    backgroundColor: 'rgb(30, 31, 33)',
  },
})

export default mainStyles